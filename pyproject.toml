[project]
name = "skill-office-reminder"
version = "0.1.0"
description = "Office reminder skill repo for the local Autobot runtime."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"